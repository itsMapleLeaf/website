{
  "name": "kingdaro-website",
  "description": "a website that hopefully looks kinda pretty",
  "version": "1.0.0",
  "author": "kingdaro",
  "license": "MIT",
  "scripts": {
    "start": "browser-sync start --server src --watch src/* --port 8080 --tunnel",
    "deploy": "surge src"
  },
  "dependencies": {},
  "devDependencies": {
    "browser-sync": "^2.23.6",
    "surge": "^0.19.0"
  }
}
